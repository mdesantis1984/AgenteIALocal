<UserControl x:Class="AgenteIALocalVSIX.ToolWindows.AgenteIALocalControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
             Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" VerticalAlignment="Center" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Agente IA Local" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
                <TextBlock Width="12" />
                <TextBlock Text="State:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="12,0,4,0" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
                <TextBlock x:Name="StateText" Text="Idle" VerticalAlignment="Center" FontFamily="Consolas" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
            </StackPanel>

            <Button Grid.Column="1"
                    x:Name="OptionsButton"
                    ToolTip="Opciones"
                    FontFamily="Segoe MDL2 Assets"
                    Content=""
                    Width="32"
                    Height="32"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="8,0,0,0"
                    Click="OptionsButton_Click" />
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Solution:" FontWeight="SemiBold" Width="80" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
            <TextBlock x:Name="SolutionNameText" Text="(no solution)" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
            <TextBlock Text="    " />
            <TextBlock Text="Projects:" FontWeight="SemiBold" Width="80" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
            <TextBlock x:Name="ProjectCountText" Text="0" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <Button x:Name="RunButton" Content="Enviar" Width="80" Click="RunButton_Click" />
            <Button x:Name="ClearButton" Content="Clear" Width="80" Margin="8,0,0,0" Click="ClearButton_Click" />
            <TextBlock x:Name="ErrorText" Foreground="Red" Margin="12,6,0,0" VerticalAlignment="Center" />
        </StackPanel>

        <GroupBox Grid.Row="3" Header="Prompt" Margin="0,0,0,8">
            <ScrollViewer>
                <TextBox x:Name="PromptTextBox" AcceptsReturn="True" Height="140" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" />
            </ScrollViewer>
        </GroupBox>

        <GroupBox Grid.Row="4" Header="Response">
            <ScrollViewer>
                <TextBox x:Name="ResponseTextBox" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" />
            </ScrollViewer>
        </GroupBox>

        <TextBlock Grid.Row="5" x:Name="LogText" TextWrapping="Wrap" FontFamily="Consolas" FontSize="11" Margin="0,8,0,0" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
    </Grid>
</UserControl>
