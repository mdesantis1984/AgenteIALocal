
    
    <Window x:Class="AgenteIALocalVSIX.ToolWindows.AgenteIALocalAboutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
    xmlns:loc="clr-namespace:AgenteIALocal.Localization;assembly=AgenteIALocal.Localization"
    xmlns:local="clr-namespace:AgenteIALocalVSIX.ToolWindows"
    Title="{loc:Translate ui.about.window.title}" Height="720" Width="1024" MinHeight="720" MinWidth="1024"
    WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
    Background="#282828"
    shell:WindowChrome.IsHitTestVisibleInChrome="True">

    <!-- Make custom chrome act as window caption for dragging -->
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="56" CornerRadius="6" ResizeBorderThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Palette / brushes -->
        <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="#FF1E1E22" />
        <SolidColorBrush x:Key="HeaderHighEmphasisBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="HeaderMediumEmphasisBrush" Color="#FF424242" />
        <SolidColorBrush x:Key="LayoutBackgroundBrush" Color="#282828" />
        <SolidColorBrush x:Key="FooterBackgroundBrush" Color="#383838" />
        <SolidColorBrush x:Key="AccentBrush" Color="#FF3FB950" />
        <SolidColorBrush x:Key="DangerBrush" Color="#FFF85149" />

        <Style x:Key="Text.Headline" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource HeaderHighEmphasisBrush}" />
        </Style>

        <Style x:Key="Text.Body" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="Foreground" Value="{StaticResource HeaderHighEmphasisBrush}" />
        </Style>

        <!-- Sidebar Navigation ToggleButton Style (copiado de ConfigWindow) -->
        <Style x:Key="SidebarNavToggleStyle" TargetType="{x:Type ToggleButton}">
          <Setter Property="OverridesDefaultStyle" Value="True" />
          <Setter Property="FocusVisualStyle" Value="{x:Null}" />
          <Setter Property="Background" Value="Transparent" />
          <Setter Property="BorderBrush" Value="Transparent" />
          <Setter Property="Foreground" Value="{StaticResource HeaderHighEmphasisBrush}" />
          <Setter Property="Padding" Value="8" />
          <Setter Property="HorizontalContentAlignment" Value="Left" />
          <Setter Property="Template">
            <Setter.Value>
              <ControlTemplate TargetType="{x:Type ToggleButton}">
                <Border x:Name="Bd"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="0"
                        CornerRadius="8"
                        Padding="12,8">
                  <ContentPresenter HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    RecognizesAccessKey="True" />
                </Border>
                <ControlTemplate.Triggers>
                  <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentBrush}" />
                    <Setter Property="Foreground" Value="White" />
                  </Trigger>

                  <MultiTrigger>
                    <MultiTrigger.Conditions>
                      <Condition Property="IsMouseOver" Value="True" />
                      <Condition Property="IsChecked" Value="False" />
                    </MultiTrigger.Conditions>
                    <Setter TargetName="Bd" Property="Background" Value="Transparent" />
                    <Setter TargetName="Bd" Property="BorderBrush" Value="Transparent" />
                    <Setter Property="Foreground" Value="{StaticResource HeaderHighEmphasisBrush}" />
                  </MultiTrigger>

                  <MultiTrigger>
                    <MultiTrigger.Conditions>
                      <Condition Property="IsPressed" Value="True" />
                      <Condition Property="IsChecked" Value="False" />
                    </MultiTrigger.Conditions>
                    <Setter TargetName="Bd" Property="Background" Value="Transparent" />
                    <Setter TargetName="Bd" Property="BorderBrush" Value="Transparent" />
                    <Setter Property="Foreground" Value="{StaticResource HeaderHighEmphasisBrush}" />
                  </MultiTrigger>

                  <MultiTrigger>
                    <MultiTrigger.Conditions>
                      <Condition Property="IsKeyboardFocusWithin" Value="True" />
                      <Condition Property="IsChecked" Value="False" />
                    </MultiTrigger.Conditions>
                    <Setter TargetName="Bd" Property="Background" Value="Transparent" />
                    <Setter TargetName="Bd" Property="BorderBrush" Value="Transparent" />
                    <Setter Property="Foreground" Value="{StaticResource HeaderHighEmphasisBrush}" />
                  </MultiTrigger>

                  <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" TargetName="Bd" Value="0.6" />
                  </Trigger>
                </ControlTemplate.Triggers>
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </Style>

        <!-- Primary Button -->
        <Style x:Key="UxPrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Height" Value="32" />
            <Setter Property="MinWidth" Value="88" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6" 
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF49C45B" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Danger Icon Button for header close -->
        <Style x:Key="UxDangerIconButton" TargetType="Button">
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                <md:PackIcon Kind="Close" Width="16" Height="16" Foreground="{StaticResource HeaderHighEmphasisBrush}" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource DangerBrush}" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource DangerBrush}" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FFB53B3B" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="6" BorderThickness="1" BorderBrush="{StaticResource HeaderMediumEmphasisBrush}" Background="{StaticResource LayoutBackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="56" />   <!-- Header -->
                <RowDefinition Height="*" />    <!-- Body -->
                <RowDefinition Height="72" />   <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border x:Name="HeaderDragArea" Grid.Row="0" Background="{StaticResource HeaderBackgroundBrush}" CornerRadius="6,6,0,0" BorderThickness="0,0,0,1" BorderBrush="{StaticResource HeaderMediumEmphasisBrush}" MouseLeftButtonDown="HeaderDragArea_MouseLeftButtonDown">
                <Grid Margin="16,0,16,0" Height="56">
                    <TextBlock Text="{loc:Translate ui.about.window.title}" 
                               Style="{StaticResource Text.Headline}" 
                               VerticalAlignment="Center"
                               HorizontalAlignment="Left"
                               Foreground="{StaticResource HeaderHighEmphasisBrush}" />
                </Grid>
            </Border>

        <!-- Body: Sidebar + Content -->
        <Grid Grid.Row="1" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Sidebar Navigation (ToggleButtons) -->
            <Border Grid.Column="0" Background="{StaticResource HeaderBackgroundBrush}" Padding="16">
                <StackPanel Orientation="Vertical" VerticalAlignment="Top">
                    <!-- Producto -->
                    <ToggleButton x:Name="NavProductToggle" IsChecked="True" Margin="0,0,0,4" Height="44" Style="{StaticResource SidebarNavToggleStyle}" Checked="NavToggle_Checked" Tag="product_name">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="Information" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="{loc:Translate ui.about.sections.productInfo}" VerticalAlignment="Center" />
                        </StackPanel>
                    </ToggleButton>

                    <!-- Créditos -->
                    <ToggleButton x:Name="NavCreditsToggle" IsChecked="False" Margin="0,0,0,4" Height="44" Style="{StaticResource SidebarNavToggleStyle}" Checked="NavToggle_Checked" Tag="credits_libraries">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="Medal" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="{loc:Translate ui.about.sections.credits}" VerticalAlignment="Center" />
                        </StackPanel>
                    </ToggleButton>

                    <!-- Repositorio -->
                    <ToggleButton x:Name="NavRepositoryToggle" IsChecked="False" Margin="0,0,0,4" Height="44" Style="{StaticResource SidebarNavToggleStyle}" Checked="NavToggle_Checked" Tag="repository_source">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="Github" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="{loc:Translate ui.about.sections.repository}" VerticalAlignment="Center" />
                        </StackPanel>
                    </ToggleButton>

                    <!-- Compatibilidad -->
                    <ToggleButton x:Name="NavCompatibilityToggle" IsChecked="False" Margin="0,0,0,4" Height="44" Style="{StaticResource SidebarNavToggleStyle}" Checked="NavToggle_Checked" Tag="compatibility_vs">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="CheckCircle" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="{loc:Translate ui.about.sections.compatibility}" VerticalAlignment="Center" />
                        </StackPanel>
                    </ToggleButton>

                    <!-- Soporte -->
                    <ToggleButton x:Name="NavSupportToggle" IsChecked="False" Margin="0,0,0,4" Height="44" Style="{StaticResource SidebarNavToggleStyle}" Checked="NavToggle_Checked" Tag="support_contact">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="HelpCircle" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="{loc:Translate ui.about.sections.support}" VerticalAlignment="Center" />
                        </StackPanel>
                    </ToggleButton>

                    <!-- Legal -->
                    <ToggleButton x:Name="NavLegalToggle" IsChecked="False" Margin="0,0,0,0" Height="44" Style="{StaticResource SidebarNavToggleStyle}" Checked="NavToggle_Checked" Tag="legal_copyright">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="ScaleBalance" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="{loc:Translate ui.about.sections.legal}" VerticalAlignment="Center" />
                        </StackPanel>
                    </ToggleButton>
                </StackPanel>
            </Border>

            <!-- Content Panel (dinámico según sección) -->
            <ScrollViewer Grid.Column="1" Padding="24" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel x:Name="ContentPanel" Orientation="Vertical">
                    <!-- Contenido dinámico (code-behind LoadSectionContent) -->
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- Footer: Close Button -->
        <Border Grid.Row="2" Background="{StaticResource FooterBackgroundBrush}" CornerRadius="0,0,6,6" BorderThickness="0,1,0,0" BorderBrush="{StaticResource HeaderMediumEmphasisBrush}" Height="72" Padding="24,12,48,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="{loc:Translate ui.about.buttons.close}" 
                            Style="{StaticResource UxPrimaryButton}" 
                            Click="CloseButton_Click" 
                            Width="120" 
                            Height="40"
                            HorizontalAlignment="Right" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
    </Border>
</Window>
